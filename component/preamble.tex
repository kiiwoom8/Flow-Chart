\documentclass[a4paper,10pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{tikz, incgraph, amsmath, amssymb,hyperref}
\usetikzlibrary{shapes,arrows,positioning,decorations.text}
\igrset{border=4cm}
\tikzset{
    rounded_rec/.append style={
            draw,
            rectangle,
            rounded corners,
            minimum width=2cm,
            minimum height=1cm
        },
    arrow_default/.append style={
            >=latex,
            ->,
            shorten >=5pt,
            shorten <=5pt
        },
    along/.style n args={1}{
            postaction={decorate,decoration={text along path, text align=center, raise=.2cm, text={#1}}}
        },
    along_r/.style n args={1}{
            postaction={decorate,decoration={text along path, reverse path, text align=center, raise=.2cm, text={#1}}}
        },
}